# LINE紹介機能 — アカウント・コンソール設定メモ（2026-06-25）

- 目的：紹介機能の実装着手前に行った **LINE側アカウント／設定の現状** と、**実装後に再開する設定手順（TODO）** を記録する。
- 方針：今回は実装（Phase 1〜）を先行し、Webhook/LIFF/リッチメニュー等の登録は**実体ができてからまとめて設定**する（＝下記 §4 を実装後に実施）。
- 関連：[20260611_LINE紹介機能_全体フロー整理.html](./20260611_LINE紹介機能_全体フロー整理.html)（§11 テスト方針・§12 LINE管理画面設定）

> 🔐 機密の取り扱い：Channel secret / アクセストークン等は**本ファイルに記載しない**（このリポジトリは機密を置かない）。値は LINE コンソールで確認し、`.env`（Git管理外）に設定する。

---

## 1. アカウント一覧（2026-06-25 時点・Playwrightで確認）

| アカウント名 | アカウントID | 友だち | プラン | 用途 |
|---|---|---|---|---|
| 【公式】京呉服平田/京呉服好一 | `@723svxpq` | 165 | ライト | **本番**（既存のLINE 1:1連携が稼働中） |
| 【社内通知用】京呉服平田/京呉服好一 | `@235cbrhw` | 1 | ライト | 社内通知用 |
| **京呉服平田【テスト】** | **`@318aqqzb`** | 0 | コミュニケーション(無料) | **★今回作成。紹介機能のローカル実機テスト用** |

- 管理者ユーザー：京呉服好一（LINEヤフーBusiness ID）
- 公式アカウントManager：https://manager.line.biz/

---

## 2. 本番アカウント @723svxpq の確認済み設定

- Messaging API：**ステータス「利用中」** ／ Channel ID `2009633542`（Channel secret は設定済・本書には非記載）
- **Webhook URL（本番）**：`https://kyogofuku-event.com/webhook/line/messaging`
- 応答方法：**「手動チャット」**（自動応答メッセージは返さない）＝ボット運用に適切
- リッチメニュー：**未作成**（今回の4項目構成が初回登録になる）

> ⚠️ 本番アカウントの Webhook は本番URLに向いている。**本番アカウントのWebhookをngrokに張り替えない**こと（本番のLINE受信が止まる／友だち165名へ誤Pushの恐れ）。ローカル実機テストは下記テスト用アカウントで行う。

---

## 3. テスト用アカウント @318aqqzb（今回作成）

- 作成日：2026-06-25 ／ 種別：未認証アカウント（無料）
- 作成時の入力内容：
  - アカウント名：京呉服平田【テスト】
  - メールアドレス：admin@kyogofuku-hirata.jp
  - 会社・事業者名：京呉服平田 ／ 所在国：日本
  - 業種：ファッション・着物・呉服
  - 運用目的：お問い合わせに対応したい、お客さんとチャットしたい
  - 主な使い方：チャット・LINEコール用
  - ビジネスマネージャー組織：新規作成（組織名「京呉服平田【テスト】」）
- 現状（2026-06-25 設定完了）：**Messaging API 有効化済（Channel ID `2010508192`）／Webhook 設定・ON済／LIFF登録済／リッチメニュー未作成**
  - Channel secret・アクセストークンは取得済 → `.env` に格納（本書には非記載）
  - 友だち追加URL（テスト）：`https://line.me/R/ti/p/@318aqqzb`

---

## 4. 実装後に行う設定（TODOチェックリスト）

> 実装（Phase 1〜3）が進み、Webhook受信・LIFF・各エンドポイントの実体ができてから実施する。

### 4-1. テスト用アカウント @318aqqzb の Messaging API
- [x] Messaging API を**有効化**（Channel ID `2010508192`）
- [x] Channel secret / チャネルアクセストークンを取得 → `.env` へ（本書には書かない）
- [x] **Webhook URL を ngrok に設定**：`https://unnagging-needingly-archimedes.ngrok-free.dev/webhook/line/messaging`
- [x] Webhook ON（※「Webhookの利用」トグルは Developers コンソールの kv-switch で、合成イベントだと戻るため**手動操作で確定**＝自動化不可）
- [ ] 応答メッセージ OFF（任意・未対応。紹介テストには影響なし）

### 4-2. LINE Login チャネル ＋ LIFF アプリ（LINE Developers コンソール）
- [x] **既存の LINE Login チャネル「顧客連携LIFF」（Channel ID `2009633621`）を流用**（新規作成せず）
- [x] LIFF アプリを新設：**「紹介・マイページ（テスト）」LIFF ID `2009633621-Wh0yxjfu`**
  - scope：openid, profile ／ サイズ：Full ／ 友だち追加オプション：On (normal)
  - ⚠️ 作成直後 scope が `profile` のみで `openid` が欠落していた → 追加。**openid が無いと `getIDToken()` が null で紐付け不可**なので必須
- [x] LIFF ID を `.env`（`LINE_REFERRAL_LIFF_ID=2009633621-Wh0yxjfu`）へ

> **🔑 重要な設計修正（LIFFパス付加が効かない件）**
> 当初は「1つのLIFFアプリ（Endpoint=`/line/liff`）＋パス付加（`liff.line.me/{id}/referral` → `/line/liff/referral`）」で4画面を賄う設計だった。
> しかし実機で **LINEがエンドポイント直下 `/line/liff` を読み込み**、`The route line/liff could not be found.` で 404 になった（パス付加が想定どおり機能しない）。
> **対処：Endpoint を実在ルート `/line/liff/referral` に変更し、画面切替は「クエリパラメータ」で行う方式に統一する。**
> - 紹介：`https://liff.line.me/2009633621-Wh0yxjfu?ref=XXXX`（`?ref=` がそのまま `/line/liff/referral?ref=XXXX` に届く＝確実）
> - 残り3画面（マイステージ／マイポイント／マイページ）は**未対応**。クエリ方式（例 `?screen=my-stage`）に集約するか、画面ごとに別LIFFアプリを発行する方針で、リッチメニュー設定時に整理する。

### 4-3. リッチメニュー（4項目に変更）【未対応・次の作業】
- [ ] 1: 友達紹介 → referral LIFF
- [ ] 2: マイステージ → my-stage LIFF
- [ ] 3: マイポイント／ギフト → my-coupons LIFF
- [ ] 4: 顧客詳細（マイページ） → mypage LIFF
- [ ] （旧 5・6 のイベント予約／問い合わせ／FAQ は廃止。レイアウトは4項目に再設計）
- [ ] 登録は `line:register-rich-menu`（詳細は `20260605_LINEリッチメニュー作成手順.md`）

### 4-4. 本番反映（Phase 6）
- [ ] 本番アカウント @723svxpq 側で LIFF/リッチメニュー等を本番ドメインで設定
- [ ] `.env` の `APP_URL` 等を本番（`https://kyogofuku-event.com`）に統一 → `config:clear`
- [ ] マイグレーション投入・告知

---

## 5. ベースURL（{ドメイン} の読み替え）

| 環境 | ベースURL |
|---|---|
| ローカル検証（ngrok） | `https://unnagging-needingly-archimedes.ngrok-free.dev` |
| 本番 | `https://kyogofuku-event.com` |

> ngrok 無料URLは再起動で変わる。変わったら本書・`.env`・LINEコンソール（Webhook/LIFF/Callback）の値を更新する。

---

## 6. 注意点

- **本番アカウント @723svxpq の Webhook は触らない**（テストは @318aqqzb で）。
- Channel secret／アクセストークンは `.env`（Git管理外）で管理。本書・コミットに平文で残さない。
- テスト用アカウントは作成時に管理者（京呉服好一）のLINEへ自動で友だち追加される。
- LIFF を使うには Messaging API（@318aqqzb）とは別に **LINE Login チャネル**が要る（§4-2）。

---

## 7. 実機テスト結果（2026-06-25 実施・全工程 PASS）

ローカル（`127.0.0.1:8002`）＋ ngrok ＋ テストBot @318aqqzb で end-to-end 検証。

### 7-1. 紹介ライフサイクル（DB／LINE実機）
1. **linked**：被紹介者がLIFF紹介リンクを踏み「紹介で登録する」→ IDトークン検証 → `referrals` 作成 ＋ お礼Push受信 ✅
   - 既存顧客（LINE紐付け済）は `existing_customer` で正しく拒否されることも確認
2. **contracted**：被紹介者を顧客化＋成約（status=確定）→ `ContractObserver` が contracted に更新 ✅
3. **matured**：`contracted_at` を据置期間（既定1か月）超過させ `php artisan referral:mature` → matured ✅
   - 紹介者へ **+6,000pt**（成約税込220,000→税抜200,000 × bronze 3%）／被紹介者へ **+10,000pt**（固定特典）
   - ステージ評価・`point_ledger` 記録・残高整合すべて一致

### 7-2. 管理画面（Playwright・運用者UI）
- 友達紹介一覧：紹介者/被紹介者/ステータス/各日付が正しく表示 ✅
- 顧客詳細「ポイント・ギフト」タブ：ステージ／成立紹介数／残高／紹介コード／状況内訳／ポイント履歴 ✅
- **キャッシュバック**：ギフトカード発行（500円）→ ポイント -500 → 残高=台帳合計で原子的整合 ✅
- ステージ設定：4ステージ閾値・還元率＋被紹介者特典pt・ギフト単位・確定月数・有効期限が管理画面から変更可 ✅

### 7-3. 検証用に作成したテストデータ（要・後始末判断）
- 顧客 `1724`（テスト紹介者（実機）・紹介コード `91BZIYLI`）／顧客 `1725`（テスト被紹介者（実機）・出題者LINE紐付け）
- contract `1630`／referral `2`（matured）／gift_card `1`（500円）／point_ledger 各種
- ※ 既存顧客 `1716`「LINE連携テスト」のLINE紐付けは、被紹介者を新規友達扱いにするため削除済（復元不要との指示）

### 7-4. 次の作業
- [ ] リッチメニュー（4項目）設定 → §4-3。あわせて残り3画面（マイステージ／マイポイント／マイページ）のクエリ方式対応
- [ ] テストデータの後始末判断
- [ ] 本番反映（Phase 6）
