---
description: テスト実行コマンド（並列・パイプ）を使わない
alwaysApply: true
---

# テスト実行について

- `php artisan test --parallel` や `php artisan test --parallel 2>&1 | head -80` のような並列実行・出力切り詰めでのテスト走行は、ユーザーが明示的に依頼しない限り行わない。
- 検証や調査のためにテストを実行する必要がある場合でも、上記パターンは使わず、ユーザーに確認するか、ユーザーが指定したコマンドに従う。
